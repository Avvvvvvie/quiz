[Instanzübersicht - OpenStack Dashboard](https://ned.cloudlab.zhaw.ch/project/)
``
`ssh -i C:\Users\anima\.ssh\bsy_2026_wed.pem ubuntu@160.85.37.175`

### In which two ways does a operating system multiplex resources?
Space and time multiplexing: the resources are shared over space/time

### How many cpus does your computer have when there are 16 cores per socket and 2 threats per core?
32

### What are the tradeoff qualities between registers, cache, main memory and a magnetic disk?
Faster <- Registers, cache, main memory, disk -> more capacity

### What is an I/O controller?
It handles talking to a specific I/O device. A driver from th eOS can then talk to its interface to get data from the device.

### Who does a driver talk to?
the I/I interface in the I/O controller.

### What is the Platform Controller Hub?
It talks to other hub controllers

### What is UEFI?

### What is a cluster?
Physical servers that are can be managed over 1 interface.

### What is an image?

### What is a terminal?
A user interface that accept text input, interprets it and executes these commands

### What is a partition?

### What items are stored per thread?
Program counter, registers, stack and state

### What is the state of a thread/process?

### What is a thread really in linux?
A linux thread is a process that shares a configurable set of resources with other processe

### What are the pro and cons of kernel-supported multi-threading?

### What is the difference between kernel supported threads and kernel threads?
kernel threads = special threads that run in kernel mode, uninteruptable, have entry in process table..

kernel supported threads = threads tht are handled and scheduled by the kernel

